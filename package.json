{
  "name": "tanstack-acp-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "TanStack AI adapter for Agent Client Protocol (ACP)",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "demo": "pnpm --filter tanstack-acp-demo dev"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
